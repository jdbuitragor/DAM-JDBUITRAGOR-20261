import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    marginBottom: 18,
  },

  label: {
    fontWeight: "700",
    marginBottom: 7,
    color: "#000",
    fontSize: 13,
  },

  input: {
    borderWidth: 1.5,
    borderColor: "#007680",
    borderRadius: 8,
    paddingVertical: 13,
    paddingHorizontal: 14,
    fontSize: 15,
    backgroundColor: "#fff",
  },
});